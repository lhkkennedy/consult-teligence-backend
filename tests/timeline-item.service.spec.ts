import timelineItemService from '../src/api/timeline-item/services/timeline-item';

describe('timeline-item service', () => {
  it('should be defined', () => {
    expect(timelineItemService).toBeDefined();
  });
});