
import articleController from '../src/api/v1/content/article/controllers/article';

describe('article controller', () => {
  it('should be defined', () => {
    expect(articleController).toBeDefined();
  });
});